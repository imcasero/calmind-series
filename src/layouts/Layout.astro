---
import Navbar from "../components/Navbar.astro";
import "../styles/global.css";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet" />
    <meta name="generator" content={Astro.generator} />
    <title>Astro Basics</title>
  </head>
  <body>
    <Navbar />
    <div class="max-w-4xl mx-auto px-4">
      <slot />
    </div>
  </body>

  <style is:global>
    :root {
      --color-jacksons-purple-300: #b9b7fb;
      --font-pokemon: 'Press Start 2P', system-ui, sans-serif;
    }
    html {
      font-family: system-ui, sans-serif;
      color: #fff;
    }

    body {
      @apply flex flex-col items-center justify-start min-h-screen;
      /* Unified background (gradient + dotted pattern) */
      background-image:
        radial-gradient(rgba(255,255,255,0.12) 1px, transparent 1px),
        linear-gradient(to bottom, var(--color-jacksons-purple-950), var(--color-jacksons-purple-800));
      background-size: 18px 18px, auto;
      background-attachment: fixed, fixed;
    }

    /* Clase para títulos estilo Pokémon */
    .pokemon-title {
      font-family: var(--font-pokemon);
      letter-spacing: 0.05em;
    }

    /* Ajustes de tamaño para títulos con fuente Pokémon */
    h1.pokemon-title {
      line-height: 1.3;
    }
  </style>
</html>
